export const metadata = {
  title: "Add Listing | House Rental Management System",
};

import NewListingClient from "./NewListingClient";

export default function NewListingPage() {
  return <NewListingClient />;
}
